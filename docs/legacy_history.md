# Legacy History

Legacy TRIM was the conceptual and technical precursor to TRIM-HAA.

TRIM-HAA is now the active project in this repository. Legacy TRIM files were removed from the active tree so ordinary users see the current package and research materials only.

Historical recovery reference:

- `legacy-trim-v0.2.1`
- commit `252f4b1c867751bd996885ec674f5f546ddbc110`

That reference preserves the frozen legacy TRIM v0.2.1 baseline before the repository transition to TRIM-HAA.
