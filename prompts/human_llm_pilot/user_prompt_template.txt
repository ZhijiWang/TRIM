STATUS: BLOCKED_NOT_EXECUTION_READY. This user prompt template is an assembly component only. It must not be used for study execution until rights, controlled private packets, model/account access, runtime settings, prompt assembly preservation, and final execution authorization pass.

Case ID: {{CASE_ID}}

Use the following frozen controlled source packet. Do not add source material, scholarship, or background knowledge beyond this packet. The same visible source layers must be used for the researcher and future model comparison.

{{SOURCE_PACKET}}

Use the model response schema below. Return only the JSON model-authored annotation payload conforming to schemas/human_llm_model_response.schema.json. Do not return runtime metadata, hashes, provider metadata, run identifiers, parse status, retry count, or review-linkage fields.

{{MODEL_RESPONSE_SCHEMA}}
