STATUS: BLOCKED_NOT_EXECUTION_READY. This user prompt template is manual/schema-compatible but must not be used for study execution until rights, controlled private packets, model/account access, and final execution authorization pass.

Case ID: {{CASE_ID}}

Use the following frozen controlled source packet. Do not add source material, scholarship, or background knowledge beyond this packet. The same visible source layers must be used for the researcher and future model comparison.

{{SOURCE_PACKET}}

Use the output schema below. Return only the JSON annotation record.

{{OUTPUT_SCHEMA}}
