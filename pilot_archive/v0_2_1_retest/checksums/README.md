# Checksums

This directory is reserved for SHA-256 manifests for frozen packages and locked return files.

Use anonymous coder IDs only. A checksum record may identify a file version and lock time, but it must not expose private coder identity.
