# Protocol Limitation Note

The Claude Opus 4.8 return is a complete AI execution bundle for protocol stress testing. It is not a human coder return and must not be used as human reliability evidence.

The return manifest reports the frozen v0.2.2 package hash and `locked=yes`. The protocol-deviation file explicitly states that the annotations were generated by an AI system and that timestamps are approximate.
