# Author–AI Comparison: IAG_JP_PUBLIC_002
## Record integrity
Both locked records use case_id `IAG_JP_PUBLIC_002`, have no parent annotation, and were frozen before this comparison. The author canonical SHA-256 is `6d78fd9d161d7a11c23ce962b257864eda16801793c6d87f17466e99ef269c50`; the AI canonical SHA-256 is `e9684ca9e776826f20647a59592caa9f6502dd471d87849b1fa76f4915e8338d`. No adjudication is performed here.

## Final-label relation
The final labels differ. The author record uses `mediated_testimony_with_unresolved_third_party_agency`; the AI record uses `self_inflicted_death_account`.

## Evidence selection
Evidence selection partially overlaps. The author record selects 5 segments; the AI record selects 7 segments. Four segments are shared across an 8-segment union, giving Jaccard overlap 0.5000, author coverage by AI 0.8000, and AI coverage by author 0.5714. The author-only segment is `IAG-JP-018`; the AI-only segments are `IAG-JP-010`, `IAG-JP-011`, and `IAG-JP-021`.

## Rationale mechanisms
The mechanisms differ in emphasis. The author foregrounds mediated testimony, perceptual limitation, and unresolved third-party intervention. The AI foregrounds self-stabbing with later dagger removal. Both records retain the self-stabbing pathway and the later unidentified intervention as materially relevant.

## Uncertainty
The uncertainty flags differ: the author record marks `high`, while the AI record marks `medium`.

## Alternative-pathway handling
Both records include an alternative pathway. The author record foregrounds unresolved third-party agency and retains self-inflicted injury with post-injury intervention as an alternative. The AI record foregrounds self-inflicted death and retains unknown removal as possible causal completion as an alternative.

The v0.2 AI record does not reproduce the earlier candidate certainty–alternative mismatch: it records medium uncertainty, explicitly preserves an alternative pathway, and identifies unknown dagger removal as possible causal completion. The primary-alternative inversion identified here is therefore a descriptive difference in pathway prioritisation, not a substitute label for that earlier mismatch.

## Shared interpretive structure
The comparison supports the classification `substantially the same two pathways with different prioritisation`. A descriptive primary-alternative inversion is identified because both records retain self-stabbing and later unidentified intervention, while foregrounding them in different primary and alternative positions.

## Unresolved differences
The records leave unresolved how much weight to place on mediated testimony, perceptual limitation, self-stabbing, dagger removal, renewed bleeding, and terminal narrative closure. This comparison records those differences without assigning a winner or revising either record.

## What this comparison supports
This comparison supports that the locked records have different final labels, partially overlapping evidence selections, different uncertainty flags, and a shared two-pathway structure with different prioritisation.

## What this comparison does not support
This comparison does not support a truth verdict, adjudication, a claim that either record should be revised, a claim about the complete causal account, or a general claim about TRIM-HAA beyond this single locked-record comparison.

This comparison cannot identify why the v0.2 result differs from the older walkthrough. Language, segmentation, prompt, model configuration, and run instance are not held constant across the two walkthroughs. Their effects are therefore confounded, and no causal or sensitivity claim can be assigned to any one factor.

The older and newer walkthroughs are non-equivalent single runs, not a controlled replication design.
